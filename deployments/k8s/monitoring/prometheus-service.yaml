apiVersion: v1
kind: Service
metadata:
  name: prometheus-service
  namespace: api-gateway
  labels:
    app: prometheus
    project: api-rate-limiter
spec:
  type: ClusterIP
  selector:
    app: prometheus
  ports:
    - port: 9090
      targetPort: 9090
      name: http